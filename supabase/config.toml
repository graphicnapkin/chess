project_id = "chess"
[functions.game]
# The handler verifies every user token with auth.getUser before database access.
verify_jwt = false
[auth]
site_url = "https://chess-iu8v.vercel.app"
additional_redirect_urls = ["http://localhost:9000/**", "http://127.0.0.1:9001/**", "https://chess-iu8v.vercel.app/**"]
